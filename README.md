# ghani-traders-erp

create folders at the root location 
- data/code
- data/mysql

run the following command will download the images and run in containers
`docker-compose up`

use docker-compose.yml file for credentials
